# Note about the nested logit model

All mathematic part behind the modeling code come from paper "specifications of nested logit model", with utility function changed to: utility = lambda * (wage * time + price).
